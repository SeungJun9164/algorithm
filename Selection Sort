선택 정렬 알고리즘

가장 작은 것을 맨 앞으로 보낸다.

int i,j,min,index,temp;
int array[10] = {1, 10, 5, 8, 7, 6, 4, 3, 2, 9};
for(i=0; i<10; i++)
{
  min = 9999;
  for(j=i; j<10; j++) 
  {
    if(min > array[j])
    {
      min = array[j];
      index = j;
      }
    }
    temp = array[i];
    array[i] = array[index];
    array[index] = temp;
  }
  return 0;
}

선택 정렬은 
= n * (n + 1) / 2
= O(n * n)
= O(n^2)

그래서 선택 정렬은 데이터의 갯수가 많아지면 처리 시간이 많이 필요하기 때문에 다른 정렬보다 비효율적이다.
