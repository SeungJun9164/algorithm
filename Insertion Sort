삽입 정렬

각 숫자를 적절한 위치에 삽입하는 방법(필요할 때만 위치 변경)

int i, j, temp;
    int array[10] = { 1,10,5,8,7,6,5,3,2,9 };
    for (i = 0; i < 10; i++)
    {
        j = i;
        while (array[j] > array[j + 1]) {
            temp = array[j];
            array[j] = array[j + 1];
            array[j + 1] = temp;
            j--;
        }
    }

    return 0;
   
삽입 정렬은 기본적으로 정렬이 되어있다고 가정!
특정한 경우에 굉장히 빠름

삽입 정렬은
= O(n*n)
= O(n^2)

시간복 O(n^2)을 가지는 정렬알고리즘 중에 가장 빠르게 수행
거의 정렬된 데이터 한해서는 어떤 알고리즘보다도 빠름
